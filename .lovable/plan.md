# Replace the app with one simple landing page

## Changes
- Replace the current feed/search experience with a clean white CoFoundersLab landing page.
- Keep only essential landing content: brand, concise headline, supporting text, and one primary action.
- Remove all secondary page routes so every URL returns to the single landing page.
- Remove the dark gradient and apply a minimal responsive white visual system.
- Update the page title and sharing description to match the simplified site.

## Technical details
- Simplify the app entry and main page instead of preserving the existing multi-page shell.
- Keep old source files unreferenced so none of their components or pages appear in the app.
- Verify the single page at desktop and mobile sizes and confirm the preview builds cleanly.
